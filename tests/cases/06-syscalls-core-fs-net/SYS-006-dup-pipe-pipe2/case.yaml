id: SYS-006
phase: 06-syscalls-core-fs-net
title: File descriptor duplication and pipe creation
class: syscall
kind: syscall_fixture
harness: syscall_fixture
meson_test: case:SYS-006

prerequisites:
  - SYS-005

expectation_authority:
  semantic_source: linux-aarch64-abi
  primary_tool: runtime-fixture
  secondary_tool: strace
  tertiary_tool: null

success_criteria:
  structural:
    - harness exits 0
    - report.json exists and valid
    - syscall_log.json exists
  semantic:
    - dup duplicates fd
    - dup2 duplicates to specific fd
    - pipe creates connected pair
    - pipe2 creates with flags

allowed_patch_scope:
  - kernel/syscall.c
  - kernel/fd.c
  - kernel/pipe.c
  - emu/aarch64/syscall.c
  - tests/cases/06-syscalls-core-fs-net/SYS-006-dup-pipe-pipe2/*

regression_target:
  meson_test: case:SYS-006

trace_defaults:
  backend: ring
  level: 2
  events: block.entry,block.exit,fault,syscall
  pc_range: null
  regs: [x0, x1, x2, x8]
  dump_on: fault

informational_only: false
status: STUB
is_gate_case: true

metadata:
  scaffolded: true
  implementation_ready: false
  notes: |
    Scaffolded case - implementation pending.
    Tests dup, dup2, pipe, and pipe2 syscalls.
