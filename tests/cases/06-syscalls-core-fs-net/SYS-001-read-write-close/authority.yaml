id: SYS-001
phase: 06-syscalls-core-fs-net

semantic_authority:
  source: linux-aarch64-abi
  reference: Linux kernel source - fs/read_write.c
  syscall_numbers:
    read: 63
    write: 64
    close: 57
  abi_conformance: aarch64-linux-gnu

decode_authority:
  instruction_type: svc
  svc_number_source: x8 register
  syscall_classes:
    - file_io

toolchain_notes:
  host_requirements:
    - POSIX-compliant operating system
    - Unix domain sockets support
    - epoll support (Linux) or kqueue (macOS fallback)
  cross_platform: partially
    # Some tests skip Linux-specific syscalls on macOS

known_gaps:
  - name: emulation_validation
    description: These tests validate host syscall behavior, not iSH emulation
    status: harness_runs_host_tests

non_authoritative_fields:
  - timestamp
  - specific_fd_numbers
  - exact_error_codes_on_edge_cases
