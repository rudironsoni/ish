id: SYS-001
phase: 06-syscalls-core-fs-net
title: Basic read, write, and close system calls
class: syscall
kind: syscall_fixture
harness: syscall_fixture
meson_test: case:SYS-001

prerequisites:
  - ELF-009

expectation_authority:
  semantic_source: linux-aarch64-abi
  primary_tool: runtime-fixture
  secondary_tool: strace
  tertiary_tool: null

success_criteria:
  structural:
    - harness exits 0
    - report.json exists and valid
    - syscall_log.json exists
  semantic:
    - read returns correct byte count
    - write outputs correct data
    - close returns 0 on success
    - fd properly invalidated after close

allowed_patch_scope:
  - kernel/syscall.c
  - kernel/fs.c
  - kernel/fd.c
  - emu/aarch64/syscall.c
  - tests/cases/06-syscalls-core-fs-net/SYS-001-read-write-close/*

regression_target:
  meson_test: case:SYS-001

trace_defaults:
  backend: ring
  level: 2
  events: block.entry,block.exit,fault,syscall
  pc_range: null
  regs: [x0, x1, x2, x8, x16]
  dump_on: fault

informational_only: false
status: REAL_PASS
is_gate_case: true

metadata:
  scaffolded: true
  implementation_ready: true
  notes: |
    Tests basic read, write, and close syscalls.
    Validates file I/O operations work correctly.
