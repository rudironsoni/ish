id: SYS-004
phase: 06-syscalls-core-fs-net

semantic_authority:
  source: linux-aarch64-abi
  reference: Linux kernel source - mm/mmap.c, mm/mprotect.c
  syscall_numbers:
    mmap: 222
    munmap: 215
    mprotect: 226
  abi_conformance: aarch64-linux-gnu
  mmap_flags:
    MAP_ANONYMOUS: 0x20
    MAP_PRIVATE: 0x02
    PROT_READ: 0x01
    PROT_WRITE: 0x02

decode_authority:
  instruction_type: svc
  syscall_classes:
    - memory_management

toolchain_notes:
  host_requirements:
    - POSIX-compliant operating system
    - mmap/munmap/mprotect support
  cross_platform: true

known_gaps:
  - name: emulation_validation
    description: These tests validate host syscall behavior, not iSH emulation
    status: harness_runs_host_tests

non_authoritative_fields:
  - timestamp
  - specific_address_returned
  - exact_page_size
