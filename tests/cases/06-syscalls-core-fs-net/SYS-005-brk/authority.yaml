id: SYS-005
phase: 06-syscalls-core-fs-net

semantic_authority:
  source: linux-aarch64-abi
  reference: Linux kernel source - mm/mmap.c
  syscall_numbers:
    brk: 214
  abi_conformance: aarch64-linux-gnu
  note: brk is deprecated on macOS; uses sbrk for testing

decode_authority:
  instruction_type: svc
  syscall_classes:
    - memory_management
    - heap_operations

toolchain_notes:
  host_requirements:
    - POSIX-compliant operating system
    - sbrk/brk support
  cross_platform: partially
    # sbrk is deprecated on macOS

known_gaps:
  - name: emulation_validation
    description: These tests validate host syscall behavior, not iSH emulation
    status: harness_runs_host_tests
  - name: macos_deprecation
    description: brk/sbrk are deprecated on macOS
    status: test_skips_shrink_on_macos

non_authoritative_fields:
  - timestamp
  - specific_break_address
