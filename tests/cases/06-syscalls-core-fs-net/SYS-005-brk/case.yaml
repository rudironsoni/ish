id: SYS-005
phase: 06-syscalls-core-fs-net
title: Heap management with brk system call
class: syscall
kind: syscall_fixture
harness: syscall_fixture
meson_test: case:SYS-005

prerequisites:
  - SYS-004

expectation_authority:
  semantic_source: linux-aarch64-abi
  primary_tool: runtime-fixture
  secondary_tool: strace
  tertiary_tool: null

success_criteria:
  structural:
    - harness exits 0
    - report.json exists and valid
    - syscall_log.json exists
  semantic:
    - brk returns current break on query
    - brk extends heap on increase
    - brk contracts heap on decrease
    - ENOMEM on excessive request

allowed_patch_scope:
  - kernel/syscall.c
  - kernel/brk.c
  - kernel/mm.c
  - emu/aarch64/syscall.c
  - tests/cases/06-syscalls-core-fs-net/SYS-005-brk/*

regression_target:
  meson_test: case:SYS-005

trace_defaults:
  backend: ring
  level: 2
  events: block.entry,block.exit,fault,syscall
  pc_range: null
  regs: [x0, x8]
  dump_on: fault

informational_only: false
status: REAL_PASS
is_gate_case: true

metadata:
  scaffolded: true
  implementation_ready: true
  notes: |
    Tests brk heap management syscall.
    Validates heap growth and shrinkage.
