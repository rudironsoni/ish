case_id: THR-002
phase: 07-threads-signals-tls
description: set_tid_address syscall operates correctly for thread ID management

expected_behavior:
  - set_tid_address syscall returns the caller's thread ID
  - Returned TID matches gettid syscall result
  - Clear child TID pointer is registered with kernel

expected_artifacts:
  report.json:
    schema: case_report_v1
    required_fields:
      - case_id: "THR-002"
      - phase: "07-threads-signals-tls"
      - harness: "thread_signal_fixture"
      - passed: true

  thread_log.json:
    schema: thread_log_v1
    required_events:
      - event: "set_tid_address"

success_criteria:
  - Harness exits with code 0
  - set_tid_address returns non-negative TID
  - TID matches gettid result
  - On macOS: test is skipped (Linux-specific syscall)
