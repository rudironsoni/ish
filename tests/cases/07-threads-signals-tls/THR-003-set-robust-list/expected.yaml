case_id: THR-003
phase: 07-threads-signals-tls
description: set_robust_list syscall for robust mutex handling

expected_behavior:
  - set_robust_list syscall registers robust mutex list head
  - Syscall returns 0 on success
  - Robust list structure is properly formatted

expected_artifacts:
  report.json:
    schema: case_report_v1
    required_fields:
      - case_id: "THR-003"
      - phase: "07-threads-signals-tls"
      - harness: "thread_signal_fixture"
      - passed: true

  thread_log.json:
    schema: thread_log_v1
    required_events:
      - event: "set_robust_list"

success_criteria:
  - Harness exits with code 0
  - set_robust_list returns 0 (success)
  - On macOS: test is skipped (Linux-specific syscall)
