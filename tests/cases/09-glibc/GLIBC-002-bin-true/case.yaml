id: GLIBC-002
phase: 09-glibc
title: /bin/true executes
class: glibc
kind: distro_fixture
harness: distro_fixture
meson_test: case:GLIBC-002

prerequisites:
  - GLIBC-001

expectation_authority:
  semantic_source: glibc-contract
  primary_tool: runtime-fixture
  secondary_tool: null
  tertiary_tool: null

success_criteria:
  structural:
    - harness exits 0
    - report.json exists and valid
  semantic:
    - test validates expected behavior

allowed_patch_scope:
  - kernel/*
  - emu/*
  - tests/cases/09-glibc/GLIBC-002-bin-true/*

regression_target:
  meson_test: case:GLIBC-002

trace_defaults:
  backend: nop
  level: 0
  events: ""
  pc_range: null
  regs: []
  dump_on: none

informational_only: false
status: REAL_PASS
is_gate_case: true

metadata:
  scaffolded: true
  implementation_ready: true
  notes: |
    Case implementation complete.
    Prerequisites: GLIBC-001 must be REAL PASS. Tests basic binary execution.
