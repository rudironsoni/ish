case_id: DISTRO-004
phase: 11-distro-matrix
description: Ubuntu package manager (apt) operations work correctly

expected_behavior:
  - apt package manager commands execute successfully
  - Package database can be queried
  - Package information can be retrieved
  - Dependency resolution works correctly
  - dpkg package management works correctly

expected_artifacts:
  report.json:
    schema: case_report_v1
    required_fields:
      - case_id: "DISTRO-004"
      - phase: "11-distro-matrix"
      - harness: "distro_fixture"
      - passed: true
      - artifacts: ["package_ops.json", "report.json"]

  package_ops.json:
    schema: package_ops_v1
    required_fields:
      - distro_name: "Ubuntu"
      - package_manager: "apt"
      - operations_tested:
          - "query"
          - "info"
          - "search"
          - "dpkg-query"

success_criteria:
  - Harness exits with code 0
  - report.json indicates passed: true
  - package_ops.json contains valid apt operations
  - All basic apt commands work as expected
  - Package database is accessible
