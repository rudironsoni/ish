case_id: DISTRO-002
phase: 11-distro-matrix
description: Alpine package manager (apk) operations work correctly

expected_behavior:
  - apk package manager commands execute successfully
  - Package database can be queried
  - Package information can be retrieved
  - Dependency resolution works correctly
  - Package installation/removal logic is validated

expected_artifacts:
  report.json:
    schema: case_report_v1
    required_fields:
      - case_id: "DISTRO-002"
      - phase: "11-distro-matrix"
      - harness: "distro_fixture"
      - passed: true
      - artifacts: ["package_ops.json", "report.json"]

  package_ops.json:
    schema: package_ops_v1
    required_fields:
      - distro_name: "Alpine Linux"
      - package_manager: "apk"
      - operations_tested:
          - "query"
          - "info"
          - "search"

success_criteria:
  - Harness exits with code 0
  - report.json indicates passed: true
  - package_ops.json contains valid apk operations
  - All basic apk commands work as expected
  - Package database is accessible
