version: "2.0"

case_id: TOOL-004
phase: 10-tooling-stability-ios
title: Archive tools work

artifacts:
  tooling_log.json:
    description: JSON log of tooling events
    schema: tooling_event_log
    required: true
  report.json:
    description: Test execution report
    schema: case_report
    required: true

success_conditions:
  structural:
    - harness exits 0
    - report.json exists and is valid JSON
    - tooling_log.json exists and is valid JSON
  semantic:
    - report.passed is true
    - tar utility is available
    - event log contains archive_tools_check event
