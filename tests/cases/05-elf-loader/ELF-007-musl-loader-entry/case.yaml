id: ELF-007
phase: 05-elf-loader
title: musl dynamic loader entry and initialization
class: elf
kind: elf_fixture
harness: elf_fixture
meson_test: case:ELF-007

prerequisites:
  - ELF-006

expectation_authority:
  semantic_source: elf-and-linux-abi
  primary_tool: runtime-fixture
  secondary_tool: llvm-objdump
  tertiary_tool: aarch64-linux-gnu-objdump

success_criteria:
  structural:
    - harness exits 0
    - report.json exists and valid
    - loader_trace.json exists
  semantic:
    - musl loader executes
    - self-relocations applied
    - program headers parsed
    - transfers to program entry

allowed_patch_scope:
  - kernel/exec.c
  - kernel/elf.c
  - emu/aarch64/*
  - tests/cases/05-elf-loader/ELF-007-musl-loader-entry/*

regression_target:
  meson_test: case:ELF-007

trace_defaults:
  backend: ring
  level: 2
  events: block.entry,block.exit,fault
  pc_range: null
  regs: [x0, x1, x2, sp]
  dump_on: fault

informational_only: false
status: STUB
is_gate_case: true

metadata:
  scaffolded: true
  implementation_ready: false
  notes: |
    Scaffolded case - implementation pending.
    Tests musl dynamic loader entry and initialization.
