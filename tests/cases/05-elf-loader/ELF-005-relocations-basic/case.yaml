id: ELF-005
phase: 05-elf-loader
title: Basic ELF relocations applied correctly
class: elf
kind: elf_fixture
harness: elf_fixture
meson_test: case:ELF-005

prerequisites:
  - ELF-004

expectation_authority:
  semantic_source: elf-and-linux-abi
  primary_tool: runtime-fixture
  secondary_tool: llvm-objdump
  tertiary_tool: aarch64-linux-gnu-objdump

success_criteria:
  structural:
    - harness exits 0
    - report.json exists and valid
    - relocations.json exists
  semantic:
    - R_AARCH64_RELATIVE applied
    - R_AARCH64_GLOB_DAT applied
    - R_AARCH64_JUMP_SLOT applied
    - relocated addresses correct

allowed_patch_scope:
  - kernel/exec.c
  - kernel/elf.c
  - kernel/reloc.c
  - emu/aarch64/*
  - tests/cases/05-elf-loader/ELF-005-relocations-basic/*

regression_target:
  meson_test: case:ELF-005

trace_defaults:
  backend: ring
  level: 2
  events: block.entry,block.exit,fault
  pc_range: null
  regs: [x0, x1, x2, x3, x4, sp]
  dump_on: fault

informational_only: false
status: STUB
is_gate_case: true

metadata:
  scaffolded: true
  implementation_ready: false
  notes: |
    Scaffolded case - implementation pending.
    Tests basic ELF relocation application.
