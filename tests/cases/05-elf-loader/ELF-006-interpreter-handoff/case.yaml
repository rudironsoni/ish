id: ELF-006
phase: 05-elf-loader
title: Interpreter handoff preserves stack and auxv
class: elf
kind: elf_fixture
harness: elf_fixture
meson_test: case:ELF-006

prerequisites:
  - ELF-005

expectation_authority:
  semantic_source: elf-and-linux-abi
  primary_tool: runtime-fixture
  secondary_tool: llvm-objdump
  tertiary_tool: aarch64-linux-gnu-objdump

success_criteria:
  structural:
    - harness exits 0
    - report.json exists and valid
    - handoff_state.json exists
  semantic:
    - stack layout preserved
    - auxv entries intact
    - AT_ENTRY points to binary
    - argc/argv/envp accessible

allowed_patch_scope:
  - kernel/exec.c
  - kernel/elf.c
  - emu/aarch64/*
  - tests/cases/05-elf-loader/ELF-006-interpreter-handoff/*

regression_target:
  meson_test: case:ELF-006

trace_defaults:
  backend: ring
  level: 2
  events: block.entry,block.exit,fault,process.entry
  pc_range: null
  regs: [x0, x1, x2, sp]
  dump_on: fault

informational_only: false
status: STUB
is_gate_case: true

metadata:
  scaffolded: true
  implementation_ready: false
  notes: |
    Scaffolded case - implementation pending.
    Tests interpreter handoff with stack and auxv preservation.
