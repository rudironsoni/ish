id: DEC-007
phase: 01-decode
title: Decode LDR/STR addressing modes correctly
class: decode
kind: decode_golden
harness: decode_golden
meson_test: case:DEC-007

prerequisites:
  - DEC-006

expectation_authority:
  semantic_source: arm-a64
  primary_tool: llvm-mc
  secondary_tool: llvm-objdump
  tertiary_tool: aarch64-linux-gnu-objdump

success_criteria:
  structural:
    - harness exits 0
    - report.json exists and valid
    - decoded.json exists
  semantic:
    - decoded fields match expected values
    - canonical disassembly matches expectation
    - all vectors pass

allowed_patch_scope:
  - emu/aarch64/decode.c
  - tests/cases/01-decode/DEC-007-ldr-str-addressing-modes/*

regression_target:
  meson_test: case:DEC-007

trace_defaults:
  backend: nop
  level: 0
  events: ""
  pc_range: null
  regs: []
  dump_on: none

informational_only: false
status: STUB
is_gate_case: true

metadata:
  scaffolded: true
  implementation_ready: false
  notes: |
    Scaffolded case - implementation pending.
    Tests LDR/STR with immediate offset, register offset, shifted register,
    pre-indexed, and post-indexed addressing modes.
    Prerequisites: DEC-006 must be REAL PASS.
