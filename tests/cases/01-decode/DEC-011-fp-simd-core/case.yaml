id: DEC-011
phase: 01-decode
title: Decode core FP and SIMD instructions correctly
class: decode
kind: decode_golden
harness: decode_golden
meson_test: case:DEC-011

prerequisites:
  - DEC-010

expectation_authority:
  semantic_source: arm-a64
  primary_tool: llvm-mc
  secondary_tool: llvm-objdump
  tertiary_tool: aarch64-linux-gnu-objdump

success_criteria:
  structural:
    - harness exits 0
    - report.json exists and valid
    - decoded.json exists
  semantic:
    - decoded fields match expected values
    - canonical disassembly matches expectation
    - all vectors pass

allowed_patch_scope:
  - emu/aarch64/decode.c
  - tests/cases/01-decode/DEC-011-fp-simd-core/*

regression_target:
  meson_test: case:DEC-011

trace_defaults:
  backend: nop
  level: 0
  events: ""
  pc_range: null
  regs: []
  dump_on: none

informational_only: false
status: STUB
is_gate_case: true

metadata:
  scaffolded: true
  implementation_ready: false
  notes: |
    Scaffolded case - implementation pending.
    Tests core floating-point and SIMD instructions (FADD, FSUB, MOV Vn, etc.).
    This is a minimal coverage gate - full FP/SIMD is beyond phase 01.
    Prerequisites: DEC-010 must be REAL PASS.
    Final gate case for phase 01-decode.
