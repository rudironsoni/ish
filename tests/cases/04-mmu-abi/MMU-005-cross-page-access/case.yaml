id: MMU-005
phase: 04-mmu-abi
title: Cross-page memory access handled correctly
class: mmu
kind: abi_fixture
harness: abi_fixture
meson_test: case:MMU-005

prerequisites:
  - MMU-004

expectation_authority:
  semantic_source: linux-aarch64-abi
  primary_tool: runtime-fixture
  secondary_tool: null
  tertiary_tool: null

success_criteria:
  structural:
    - harness exits 0
    - report.json exists and valid
    - memory_access.json exists
  semantic:
    - unaligned cross-page access works
    - multi-byte access spanning pages handled
    - permissions checked for both pages
    - no spurious faults

allowed_patch_scope:
  - kernel/mm.c
  - kernel/fault.c
  - emu/aarch64/cpu.c
  - emu/aarch64/mem.c
  - tests/cases/04-mmu-abi/MMU-005-cross-page-access/*

regression_target:
  meson_test: case:MMU-005

trace_defaults:
  backend: ring
  level: 2
  events: fault,block.entry,block.exit,syscall
  pc_range: null
  regs: [x0, x1, x2, sp]
  dump_on: fault

informational_only: false
status: REAL_PASS
is_gate_case: true

metadata:
  scaffolded: true
  implementation_ready: true
  notes: |
    Cross-page memory access test implemented.
    Validates multi-page mmap and permission isolation.
