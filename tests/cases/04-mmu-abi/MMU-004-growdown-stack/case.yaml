id: MMU-004
phase: 04-mmu-abi
title: Stack grows downward on access
class: mmu
kind: abi_fixture
harness: abi_fixture
meson_test: case:MMU-004

prerequisites:
  - MMU-003

expectation_authority:
  semantic_source: linux-aarch64-abi
  primary_tool: runtime-fixture
  secondary_tool: null
  tertiary_tool: null

success_criteria:
  structural:
    - harness exits 0
    - report.json exists and valid
    - stack_trace.json exists
  semantic:
    - stack accessible at initial sp
    - stack grows on push/decrement
    - guard page prevents overrun
    - stack contents preserved

allowed_patch_scope:
  - kernel/mm.c
  - kernel/fault.c
  - emu/aarch64/cpu.c
  - tests/cases/04-mmu-abi/MMU-004-growdown-stack/*

regression_target:
  meson_test: case:MMU-004

trace_defaults:
  backend: ring
  level: 2
  events: fault,block.entry,block.exit,syscall
  pc_range: null
  regs: [sp, x0, x1, x2]
  dump_on: fault

informational_only: false
status: STUB
is_gate_case: true

metadata:
  scaffolded: true
  implementation_ready: false
  notes: |
    Scaffolded case - implementation pending.
    Tests growdown stack behavior.
