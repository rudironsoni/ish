version: "1"
description: "Minimal fixture for EXEC-011 - no external binaries needed"

fixtures:
  - name: test_memory
    type: ephemeral
    description: "64KB test memory allocated by harness"
    ephemeral_base: 0x1000
    size: 65536

provenance: "Generated by harness at runtime"

checksum:
  method: none
  value: null

notes: |
  This case has no persistent fixtures.
  Test memory is initialized by semantic_micro_harness.
  TCTI gadgets are generated by tcti/aarch64/gen.c.
