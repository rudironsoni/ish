id: EXEC-013
phase: 03-semantic-exec
title: CMP instruction sets Z=0 when operands are not equal
class: semantic
kind: semantic_micro
harness: semantic_micro
meson_test: case:EXEC-013

prerequisites:
  - EXEC-002
  - EXEC-012

expectation_authority:
  semantic_source: arm-a64
  primary_tool: llvm-mc

success_criteria:
  structural:
    - harness exits 0
    - report.json exists and valid
  semantic:
    - CMP with unequal operands sets Z=0
    - N flag correctly reflects sign of result
    - PC advanced correctly

trace_defaults:
  backend: ring
  level: 2
  events: block.entry,block.exit
  regs: [x0, x1, nzcv]

informational_only: false
status: pending

metadata:
  scaffolded: true
  implementation_ready: true
  notes: |
    Tests CMP sets Z=0 when operands differ.
    Required for correct B.NE taken path.
