id: EXEC-019
phase: 03-semantic-exec
title: STR XZR with post-index updates base register
class: semantic
kind: semantic_micro
harness: semantic_micro
meson_test: case:EXEC-019

prerequisites:
  - EXEC-003

expectation_authority:
  semantic_source: arm-a64
  primary_tool: llvm-mc

success_criteria:
  structural:
    - harness exits 0
  semantic:
    - STR stores zero to memory
    - Post-index updates base register
    - Base incremented by correct offset

trace_defaults:
  backend: ring
  level: 2
  events: block.entry,block.exit
  regs: [x2]

informational_only: false
status: pending

metadata:
  scaffolded: true
  notes: |
    Post-index STR updates base after store.
    Pattern: str xzr, [x2], #8
    Critical for interpreter loop.
