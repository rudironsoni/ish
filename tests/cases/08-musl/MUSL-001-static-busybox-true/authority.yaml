case_id: MUSL-001
authority_type: distro_fixture

semantic_authority:
  source: musl libc project
  document: Static linking and binary execution
  url: https://musl.libc.org/
  stable_reference: true

decode_authority:
  not_applicable: true
  reason: Runtime fixture test - no instruction decode required

generator_authority:
  not_applicable: true
  reason: Runtime fixture test - no code generation required

abi_authority:
  source: Linux AArch64 ABI
  reference: ARM Architecture Procedure Call Standard (AAPCS)
  static_binary: No dynamic linker involvement

expectation_source:
  - Static binaries link all required libc code at build time
  - musl libc supports fully static linking
  - Busybox static binary should run without shared libraries

known_gaps:
  - On macOS, tests run in informational mode (musl is Linux-specific)
  - Full validation requires Alpine Linux environment with static busybox

non_authoritative_fields:
  - Specific busybox applet behavior
  - Static binary size optimization details
