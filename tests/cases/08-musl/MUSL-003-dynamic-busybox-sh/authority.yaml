case_id: MUSL-003
authority_type: distro_fixture

semantic_authority:
  source: BusyBox project
  document: Shell applet functionality
  url: https://busybox.net/
  stable_reference: true

decode_authority:
  not_applicable: true
  reason: Runtime fixture test - no instruction decode required

generator_authority:
  not_applicable: true
  reason: Runtime fixture test - no code generation required

abi_authority:
  source: Linux AArch64 ABI
  reference: ARM Architecture Procedure Call Standard (AAPCS)
  shell_execution: Fork/exec for command execution

expectation_source:
  - Busybox shell provides POSIX-compatible shell
  - Shell can execute both builtins and external commands
  - Command exit status propagates correctly

known_gaps:
  - On macOS, tests run in informational mode
  - Full validation requires Alpine Linux with busybox

non_authoritative_fields:
  - Specific shell builtin implementations
  - Interactive shell features (job control, history)
