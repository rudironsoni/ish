# Authority Chain for GLIBC-006
case_id: "GLIBC-006"
phase: "09-glibc"

semantic_authority:
  glibc_compatibility: "GNU C Library (glibc) provides standard C library and POSIX functions"
  debian_ecosystem: "Debian/Ubuntu package ecosystem compatibility"
  linux_standard_base: "LSB compliance for dynamic linking and system interfaces"

toolchain_notes:
  cross_compilation: "AArch64 binaries require cross-compilation toolchain"
  test_environment: "Real validation requires Linux environment with glibc"
  macos_limitation: "macOS lacks glibc - tests run in informational mode"

known_gaps:
  - "Actual glibc binary execution requires Linux environment"
  - "Dynamic loader validation requires AArch64 glibc binaries"
  - "Package manager tests require Debian/Ubuntu chroot or container"

non_authoritative_fields:
  - "execution_log.json content varies by platform"
  - "Fixture availability depends on test environment setup"
