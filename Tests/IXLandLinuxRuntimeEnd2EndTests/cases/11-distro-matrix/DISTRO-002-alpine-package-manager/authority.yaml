case_id: DISTRO-002
authority_type: distro_fixture

semantic_authority:
  source: Alpine Linux Documentation
  document: Alpine Package Keeper (apk) Manual
  url: https://wiki.alpinelinux.org/wiki/Alpine_Linux_package_management
  stable_reference: true

decode_authority:
  not_applicable: true
  reason: Package manager test - no instruction decode required

generator_authority:
  not_applicable: true
  reason: Package manager test - no code generation required

abi_authority:
  source: musl libc project
  reference: musl libc syscall and ABI compatibility
  notes: apk uses musl-specific paths and features

elf_authority:
  source: Alpine Linux musl dynamic linker
  reference: /lib/ld-musl-aarch64.so.1
  notes: Alpine packages link against musl libc

expectation_source:
  - apk is the Alpine Package Keeper
  - apk uses /etc/apk/repositories for package sources
  - Package database is in /var/lib/apk
  - apk supports operations: add, del, update, upgrade, search, info
  - musl-compatible packages are required for Alpine

known_gaps:
  - On macOS host, tests use mock fixtures
  - Actual package installation is not performed
  - Network repository access is simulated

non_authoritative_fields:
  - Specific package versions in repositories
  - Network latency and repository availability
  - Custom repository configurations
