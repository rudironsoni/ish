case_id: DISTRO-003
authority_type: distro_fixture

semantic_authority:
  source: Ubuntu Documentation
  document: Ubuntu Server Guide
  url: https://ubuntu.com/server/docs
  stable_reference: true

decode_authority:
  not_applicable: true
  reason: Distro compatibility test - no instruction decode required

generator_authority:
  not_applicable: true
  reason: Distro compatibility test - no code generation required

abi_authority:
  source: GNU C Library (glibc)
  reference: glibc Linux ABI compatibility
  notes: Ubuntu uses glibc as its C library

elf_authority:
  source: Ubuntu glibc dynamic linker
  reference: /lib/aarch64-linux-gnu/ld-linux-aarch64.so.1

expectation_source:
  - Ubuntu uses glibc as its C library
  - Package managers are apt/apt-get/dpkg
  - Default shell is bash
  - System follows Debian package management conventions
  - glibc provides full POSIX and Linux ABI compatibility

known_gaps:
  - On macOS host, tests use mock fixtures rather than real Ubuntu system
  - Full validation requires running on actual Ubuntu AArch64
  - Package installation tests are simulated

non_authoritative_fields:
  - Specific package versions in Ubuntu repositories
  - Exact apt command output formatting
  - System configuration variations between Ubuntu releases
