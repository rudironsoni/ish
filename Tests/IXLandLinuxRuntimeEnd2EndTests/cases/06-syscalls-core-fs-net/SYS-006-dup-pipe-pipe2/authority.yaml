id: SYS-006
phase: 06-syscalls-core-fs-net

semantic_authority:
  source: linux-aarch64-abi
  reference: Linux kernel source - fs/file.c, fs/pipe.c
  syscall_numbers:
    dup: 23
    dup2: 24
    pipe: 59
    pipe2: 59
  abi_conformance: aarch64-linux-gnu
  note: pipe2 is Linux-specific; dup2 used on macOS

decode_authority:
  instruction_type: svc
  syscall_classes:
    - fd_operations
    - pipe_operations

toolchain_notes:
  host_requirements:
    - POSIX-compliant operating system
    - pipe support
  cross_platform: partially
    # pipe2 is Linux-specific

known_gaps:
  - name: emulation_validation
    description: These tests validate host syscall behavior, not iSH emulation
    status: harness_runs_host_tests
  - name: macos_pipe2
    description: pipe2 is Linux-specific
    status: test_skips_pipe2_on_macos

non_authoritative_fields:
  - timestamp
  - specific_fd_numbers
