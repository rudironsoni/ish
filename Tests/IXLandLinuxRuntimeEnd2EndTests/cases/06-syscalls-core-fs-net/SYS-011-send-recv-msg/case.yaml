id: SYS-011
phase: 06-syscalls-core-fs-net
title: Socket data transfer with send, recv, and message variants
class: syscall
kind: syscall_fixture
harness: syscall_fixture
meson_test: case:SYS-011

prerequisites:
  - SYS-010

expectation_authority:
  semantic_source: linux-aarch64-abi
  primary_tool: runtime-fixture
  secondary_tool: strace
  tertiary_tool: null

success_criteria:
  structural:
    - harness exits 0
    - report.json exists and valid
    - syscall_log.json exists
  semantic:
    - send transmits data
    - recv receives data
    - sendto/recvfrom with addresses
    - sendmsg/recvmsg with control

allowed_patch_scope:
  - kernel/syscall.c
  - kernel/socket.c
  - kernel/net.c
  - emu/aarch64/syscall.c
  - tests/cases/06-syscalls-core-fs-net/SYS-011-send-recv-msg/*

regression_target:
  meson_test: case:SYS-011

trace_defaults:
  backend: ring
  level: 2
  events: block.entry,block.exit,fault,syscall
  pc_range: null
  regs: [x0, x1, x2, x3, x4, x5, x8]
  dump_on: fault

informational_only: false
status: REAL_PASS
is_gate_case: true

metadata:
  scaffolded: true
  implementation_ready: true
  notes: |
    Tests send, recv, sendto, recvfrom, sendmsg, recvmsg syscalls.
    Validates socket data transfer operations.
