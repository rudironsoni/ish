id: SYS-012
phase: 06-syscalls-core-fs-net
title: File renaming and linking operations
class: syscall
kind: syscall_fixture
harness: syscall_fixture
meson_test: case:SYS-012

prerequisites:
  - SYS-011

expectation_authority:
  semantic_source: linux-aarch64-abi
  primary_tool: runtime-fixture
  secondary_tool: strace
  tertiary_tool: null

success_criteria:
  structural:
    - harness exits 0
    - report.json exists and valid
    - syscall_log.json exists
  semantic:
    - rename moves files
    - link creates hard links
    - unlink removes files
    - symlink creates symbolic links
    - readlink reads symlink targets

allowed_patch_scope:
  - kernel/syscall.c
  - kernel/fs.c
  - kernel/namei.c
  - emu/aarch64/syscall.c
  - tests/cases/06-syscalls-core-fs-net/SYS-012-rename-link-symlink-readlink/*

regression_target:
  meson_test: case:SYS-012

trace_defaults:
  backend: ring
  level: 2
  events: block.entry,block.exit,fault,syscall
  pc_range: null
  regs: [x0, x1, x2, x3, x4, x5, x8]
  dump_on: fault

informational_only: false
status: REAL_PASS
is_gate_case: true

metadata:
  scaffolded: true
  implementation_ready: true
  notes: |
    Tests rename, link, unlink, symlink, readlink syscalls.
    Validates filesystem operations.
