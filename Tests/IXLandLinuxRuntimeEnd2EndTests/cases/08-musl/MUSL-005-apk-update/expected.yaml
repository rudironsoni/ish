case_id: MUSL-005
phase: 08-musl
description: Alpine package manager (apk) can update package index

expected_behavior:
  - apk binary executes without errors
  - Package index update completes
  - Network connectivity works for package download
  - Local package database is updated

expected_artifacts:
  report.json:
    schema: case_report_v1
    required_fields:
      - case_id: "MUSL-005"
      - phase: "08-musl"
      - harness: "distro_fixture"
      - passed: true
      - artifacts: ["distro_log.json", "report.json"]

  distro_log.json:
    schema: distro_log_v1
    required_events:
      - event: "apk_update_test"

success_criteria:
  - Harness exits with code 0
  - report.json indicates passed: true
  - distro_log.json contains expected event
  - Package manager functions correctly
