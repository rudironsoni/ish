case_id: MUSL-003
phase: 08-musl
description: Dynamic busybox shell runs commands correctly

expected_behavior:
  - Busybox sh applet starts successfully
  - Shell can execute basic commands
  - Shell exits cleanly with correct status
  - Interactive shell features work

expected_artifacts:
  report.json:
    schema: case_report_v1
    required_fields:
      - case_id: "MUSL-003"
      - phase: "08-musl"
      - harness: "distro_fixture"
      - passed: true
      - artifacts: ["distro_log.json", "report.json"]

  distro_log.json:
    schema: distro_log_v1
    required_events:
      - event: "busybox_shell_test"

success_criteria:
  - Harness exits with code 0
  - report.json indicates passed: true
  - distro_log.json contains expected event
  - Shell command execution validated
