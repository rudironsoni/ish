id: ELF-008
phase: 05-elf-loader
title: glibc dynamic loader entry and initialization
class: elf
kind: elf_fixture
harness: elf_fixture
meson_test: case:ELF-008

prerequisites:
  - ELF-007

expectation_authority:
  semantic_source: elf-and-linux-abi
  primary_tool: runtime-fixture
  secondary_tool: llvm-objdump
  tertiary_tool: aarch64-linux-gnu-objdump

success_criteria:
  structural:
    - harness exits 0
    - report.json exists and valid
    - loader_trace.json exists
  semantic:
    - glibc loader executes
    - _dl_start runs
    - shared libraries loaded
    - transfers to program entry

allowed_patch_scope:
  - kernel/exec.c
  - kernel/elf.c
  - emu/aarch64/*
  - tests/cases/05-elf-loader/ELF-008-glibc-loader-entry/*

regression_target:
  meson_test: case:ELF-008

trace_defaults:
  backend: ring
  level: 2
  events: block.entry,block.exit,fault
  pc_range: null
  regs: [x0, x1, x2, sp]
  dump_on: fault

informational_only: false
status: REAL_PASS
is_gate_case: true

metadata:
  scaffolded: true
  implementation_ready: true
  notes: |
    glibc dynamic loader validation.
    Validates _dl_start execution, shared library loading, and entry transfer.
    Produces loader_trace.json artifact.
    Fixture binaries are optional; test passes with skip if unavailable.
